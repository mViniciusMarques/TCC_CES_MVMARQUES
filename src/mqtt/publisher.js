const mqtt_client = require('./mqtt-client.js');
const mqtt_options = require('./mqtt-client.js');
const mqtt_connect = require ('./mqtt-client.js').mqttConnect;

// console.log(typeof(mqtt_connect))

function publisher(topic,msg,options) {

    // if(mqtt_client.connected()) {
    //     mqtt_client.publish()
        
    // }

}

module.exports = publisher;